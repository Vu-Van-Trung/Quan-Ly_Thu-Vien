# Tasks

1.  **Modify `FormFine.cs` (InitializeComponent)**: - [x]
    -   Set `dgvBooks.AutoSizeColumnsMode = DataGridViewAutoSizeColumnsMode.Fill`.
    -   Set `dgvFines.AutoSizeColumnsMode = DataGridViewAutoSizeColumnsMode.Fill`.
2.  **Verify**: - [x]
    -   Run the app and check if columns expand to fill the width of the grid.
