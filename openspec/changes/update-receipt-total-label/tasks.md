# Tasks

- [x] Update receipts generation logic in `FormFine.cs` to print "Tổng thanh toán" instead of "Tổng tiền phạt". <!--id: update-label-->
- [x] Ensure the total amount uses explicit VND formatting (e.g., `C0` with `vi-VN`). <!--id: verify-fmt-->
