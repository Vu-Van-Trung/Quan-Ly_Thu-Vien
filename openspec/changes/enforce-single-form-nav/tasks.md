# Tasks

- [x] Update `FormLoan.cs`: In `btnTraSach_Click`, ensure `this.Close()` is called after opening `FormFine` (and ensure `FormFine` is properly attached to MDI Parent). <!--id: loan-nav-->
- [x] Verify `FormFine.cs` receipt logic: Standardize "Tổng thanh toán" formatting one last time to ensure it strictly meets `C0` `vi-VN` format and includes all fine components. <!--id: verify-receipt-->
